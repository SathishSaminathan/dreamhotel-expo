import React from 'react';
import {Text, View, TextInput} from 'react-native';

const TextInputComponent = (props) => (
  <TextInput {...props} style={{fontFamily:"Proxima Nova Regular"}} />
);

export default TextInputComponent;
